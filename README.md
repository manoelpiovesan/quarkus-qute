# Server-side Rendering with Quarkus Qute


Create a .env file in the root of the project with the following content:

```properties
DB_URL=jdbc:postgresql://localhost:5432/quarkus_qute
DB_USERNAME=quarkus
DB_PASSWORD=Qu4rku5
```
